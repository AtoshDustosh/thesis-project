- command lines:
  bwa index data/test.fa

  bwa mem data/test.fa data/test.fastq > data/test.sam

  samtools sort data/test.sam -o data/test.sam --output-fmt SAM

  gcc *.c -o main -lhts

  // add a command line to filter bad mapped reads

  ./main --faFile data/test.fa --fastqFile data/test.fastq --samFile data/test.sam --vcfFile data/test.vcf --outputFile data/grbvOut.sam --integrateVcfToSam

- TODOs:
  1. manipulation of SVs with special formats
  2. manipulation based on haplotypes
  3. refactor the loading program for *.fa/fna files. Some files of this type don't have the field "LN" in their info lines.
